var EventEmitter = require('events').EventEmitter;

/* Events for nico
 *
 * + new
 * + changed
 *
 * + loading
 * + loaded
 * + reading
 * + read
 * + indexing
 * + indexed
 */

exports = module.exports = new EventEmitter();
